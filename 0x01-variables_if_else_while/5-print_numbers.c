#include <stdio.h>

/**
 * main - A program to print a number
 * Return: always 0
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("thi numbers are: %d", a);
	printf('\n');
	return (0);
}
